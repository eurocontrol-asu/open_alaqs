# Modern GSE Application (CSV version)

## Setup

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

2. Compile UI files (using Qt Designer):
   ```
   pyuic5 views/ui_main.ui -o views/ui_main.py
   ```

3. Run the application:
   ```
   python gse.py
   ```
